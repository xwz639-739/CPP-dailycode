#include"CharacterCount.h"

int main()
{
	/*string filename;
	ifstream file(filename.c_str());
	cout << "please enter the file: ";
	cin >> filename;*/
	ifstream cin(test.txt);
	runQueries();
	return 0;
}